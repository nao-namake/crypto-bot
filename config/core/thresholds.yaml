# ========================================
# 動的閾値設定ファイル (thresholds.yaml)
# ========================================
# 目的: ML統合・リスク管理・戦略パラメータの動的閾値一元管理
#
# このファイルの役割:
#   1. ML統合パラメータ（信頼度閾値・重み・ボーナス/ペナルティ）
#   2. 動的信頼度計算（市場不確実性調整・戦略別信頼度範囲）
#   3. 戦略パラメータ（5戦略の閾値・信頼度計算）
#   4. リスク管理設定（Kelly基準・ドローダウン・証拠金維持率）
#   5. ポジション管理設定（TP/SL・緊急決済）
#   6. Optuna最適化パラメータ（Phase 40最適化結果）
#
# 設定ファイル分離:
#   - features.yaml: 機能オンオフ
#   - unified.yaml: 基本設定（exchange、mode等）
#   - thresholds.yaml: 動的閾値（このファイル）
#   - feature_order.json: 特徴量定義
#
# 使用方法:
#   - get_threshold()関数で動的に参照（ハードコード禁止）
#   - Optuna最適化: scripts/optimization/run_phase40_optimization.py
#
# ========================================

# ========================================
# 実行設定（Execution Settings）
# ========================================
execution:
  backtest_period_days: 180  # Phase 51.8-10: フル期間バックテスト（理論的設定値検証）
  live_mode_interval_seconds: 300
  paper_mode_interval_seconds: 300

# ========================================
# バックテスト設定（Backtest Settings）
# ========================================
backtest:
  log_level: WARNING
  discord_enabled: false
  data_limit: 10000  # Phase 51.10-C: サンプル数上限（10,000件で停止）
  progress_interval: 100  # Phase 51.10-C: 進捗表示間隔（100件毎・1000→100に改善）
  report_interval: 10000
  mock_api_calls: true
  enable_detailed_logging: false
  fast_data_slicing: true
  data_sampling_ratio: 1.0  # Phase 51.8-10: 全データ使用（backtest_period_daysで期間制限）
  inner_loop_count: 1  # Phase 51.8-J4-H: 高速化（1回実行・約40分）注: Phase 51.8-K完了後、最終検証時は3に戻す

# ========================================
# 🤖 ML統合設定（ML Integration Settings）
# ========================================
# Phase 41.8.5最適化完了:
#   - min_ml_confidence: 0.45（3クラス分類対応）
#   - high_confidence_threshold: 0.60（3クラス分類対応）
#   - ML統合率100%達成（10%→100%改善）
#   - 3段階統合ロジック（<0.45: 戦略のみ・0.45-0.60: 加重平均・≥0.60: ボーナス/ペナルティ）
# ========================================
ml:
  confidence_threshold: 0.3
  allow_dummy_fallback: false
  default_confidence: 0.5
  strategy_integration:
    enabled: true
    ml_weight: 0.35
    strategy_weight: 0.7
    high_confidence_threshold: 0.6
    agreement_bonus: 1.2
    disagreement_penalty: 0.90  # Phase 51.8-J2: ペナルティ緩和（0.85→0.90）
    min_ml_confidence: 0.35      # Phase 51.8-J2: 閾値緩和（0.45→0.35）
    hold_conversion_threshold: 0.30  # Phase 51.8-J2: hold変換を厳しく（0.40→0.30）
  # Meta-Learning動的重み最適化システム
  meta_learning:
    enabled: false  # デフォルト無効（慎重な段階的有効化）
    model_path: "models/meta_learning/meta_model.pkl"
    # フォールバック固定重み（Meta-ML失敗時使用）
    fallback_ml_weight: 0.35
    fallback_strategy_weight: 0.7

  # ========================================
  # Phase 51.9: レジーム別ML統合設定
  # ========================================
  # Phase 51.8バックテスト結果に基づく最適化:
  #   - tight_range: 利益の99.4%寄与 → 戦略重視・ML補完控えめ
  #   - normal_range: 利益の0.6%寄与・PF=1.005 → バランス型
  #   - trending: 未発生だが将来対応 → ML補完重視
  #   - high_volatility: エントリー禁止 → 超高閾値
  # ========================================
  regime_ml_integration:
    enabled: true  # Phase 51.9: レジーム別ML統合有効化

    # tight_range（狭いレンジ）: 戦略重視型
    # Phase 51.8結果: 1,380件（91.8%）、PF=1.065、利益寄与99.4%
    # → 戦略シグナルが強い → ML補完は控えめ
    tight_range:
      min_ml_confidence: 0.50           # 高閾値（戦略重視）
      high_confidence_threshold: 0.65
      ml_weight: 0.25                   # ML 25%
      strategy_weight: 0.75             # 戦略 75%
      agreement_bonus: 1.15             # ML・戦略一致時ボーナス控えめ
      disagreement_penalty: 0.92        # ML・戦略不一致時ペナルティ緩和

    # normal_range（通常レンジ）: バランス型
    # Phase 51.8結果: 124件（8.2%）、PF=1.005、利益寄与0.6%
    # → ほぼbreakeven → MLとのバランス重視
    normal_range:
      min_ml_confidence: 0.40           # 中閾値（バランス型）
      high_confidence_threshold: 0.60
      ml_weight: 0.30                   # ML 30%
      strategy_weight: 0.70             # 戦略 70%
      agreement_bonus: 1.20             # ML・戦略一致時ボーナス標準
      disagreement_penalty: 0.90        # ML・戦略不一致時ペナルティ標準

    # trending（トレンド）: ML補完重視型
    # Phase 51.8結果: 0件（未発生）
    # → 実験的設定・ML補完を積極活用
    trending:
      min_ml_confidence: 0.35           # 低閾値（ML補完重視）
      high_confidence_threshold: 0.55
      ml_weight: 0.35                   # ML 35%
      strategy_weight: 0.65             # 戦略 65%
      agreement_bonus: 1.25             # ML・戦略一致時ボーナス強化
      disagreement_penalty: 0.88        # ML・戦略不一致時ペナルティやや強化

    # high_volatility（高ボラティリティ）: 超保守型
    # Phase 51.8結果: 0件（未発生・ポジション制限0でエントリー禁止）
    # → 超高閾値でほぼエントリーなし
    high_volatility:
      min_ml_confidence: 0.80           # 超高閾値（実質エントリーなし）
      high_confidence_threshold: 0.90
      ml_weight: 0.10                   # ML 10%
      strategy_weight: 0.90             # 戦略 90%
      agreement_bonus: 1.10             # ボーナス最小
      disagreement_penalty: 0.85        # ペナルティ最強化

  # Meta-ML予測信頼度閾値
  min_confidence: 0.3
  # 市場状況特徴量設定
  market_features:
    # ボラティリティ関連
    atr_window: 14
    bb_window: 20
    volatility_ratio_window: 7
    # トレンド関連
    ema_short: 20
    ema_long: 50
    adx_window: 14
    trend_strength_threshold: 0.5
    # レンジ判定関連
    donchian_window: 20
    range_threshold: 0.02
  # パフォーマンストラッキング設定
  performance_tracking:
    window_days_short: 7
    window_days_long: 30
    min_trades_required: 5
    history_file: "src/core/state/performance_history.json"
  # Meta-MLモデル設定
  model_config:
    n_estimators: 100
    learning_rate: 0.05
    max_depth: 5
    random_state: 42
  dynamic_confidence:
    base_hold: 0.35
    error_fallback: 0.25
    ml_fallback: 0.35
    neutral_default: 0.4
    strategy_fallback: 0.35
    market_uncertainty:
      volatility_factor_max: 0.1
      volatility_divisor: 1.0
      volume_factor_max: 0.06
      volume_multiplier: 0.2
      price_factor_max: 0.04
      uncertainty_max: 0.2
      uncertainty_boost: 1.0
    strategies:
      # DonchianChannel戦略動的信頼度設定
      donchian_channel:
        breakout_base: 0.6
        breakout_min: 0.3
        breakout_max: 0.85
        reversal_base: 0.5
        reversal_min: 0.3
        reversal_max: 0.7
        hold_base: 0.4
        hold_min: 0.35
        hold_max: 0.6
        volume_bonus_max: 0.1
        volume_multiplier: 0.2

      # ADXTrend戦略動的信頼度設定（Phase 51.9-5: インデント修正）
      adx_trend:
        strong_base: 0.65
        strong_min: 0.4
        strong_max: 0.85
        moderate_base: 0.5
        moderate_min: 0.35
        moderate_max: 0.7
        weak_base: 0.35
        weak_min: 0.25
        weak_max: 0.5
        hold_base: 0.45
        hold_min: 0.35
        hold_max: 0.6
        default_min: 0.25
        default_max: 0.6
        adx_bonus_max: 0.25
        adx_divisor: 25
        di_bonus_max: 0.15
        di_divisor: 12
        volume_bonus_max: 0.1
        volume_multiplier: 0.15

      # ATRBased戦略動的信頼度設定（Phase 51.9-5: strategies配下に移動）
      atr_based:
        high_volatility_base: 0.55
        high_volatility_min: 0.35
        high_volatility_max: 0.8
        normal_volatility_base: 0.45
        normal_volatility_min: 0.3
        normal_volatility_max: 0.65
        low_volatility_base: 0.35
        low_volatility_min: 0.25
        low_volatility_max: 0.5
        rsi_base: 0.2
        rsi_multiplier: 0.3
        agreement_max: 0.85
        weak_base: 0.08
        weak_multiplier: 0.1
        volatility_bonus: 1.02
        volatility_max: 0.85
        position_size_base: 0.015       # ← ML訓練で参照（Phase 51.9-5: 階層修正で解決）
        market_stress_threshold: 0.7
        min_atr_ratio: 0.5
        strength_divisor: 0.2
        strength_normalize: 30.0
  emergency_stop_on_dummy: true
  max_model_failures: 3
  prediction_fallback_confidence: 0.0
  dummy_confidence: 0.5
  model_paths:
    base_path: /app
    local_path: .
    training_path: models/training

# ========================================
# 📊 動的信頼度計算（Dynamic Confidence Calculation）
# ========================================
# 市場不確実性に基づく信頼度調整・戦略別信頼度範囲設定
# ========================================
performance:
  default_latency_ms: 100.0

# ========================================
# 🎯 動的戦略選択設定（Dynamic Strategy Selection）
# ========================================
# Phase 51.5-A: 3戦略構成最適化（MochipoyAlert・MultiTimeframe削除）
#
# 目的: 市場レジーム（tight_range/normal_range/trending/high_volatility）に応じて
#       戦略の重みを動的に変更し、レジーム特性に最適化された戦略選択を実現
#
# 設定方法:
#   - レジーム名: tight_range, normal_range, trending, high_volatility
#   - 戦略名: ATRBased, BBReversal, DonchianChannel, StochasticReversal,
#             ADXTrendStrength, MACDEMACrossover（6戦略）
#   - 重み: 0.0-1.0（合計1.0になるように設定）
#   - high_volatility: 全戦略無効化（全て0.0）
# ========================================
dynamic_strategy_selection:
  enabled: true  # Phase 51.8-J2: レジーム別戦略重み最適化有効化（バックテスト評価）
  # レジーム別戦略重みマッピング
  # Phase 51.8: 6戦略統合・レジーム別重み初期値（バックテスト最適化前）
  regime_strategy_mapping:
    tight_range:  # 狭いレンジ相場（BB幅 < 3%, 価格変動 < 2%）
      # Phase 54.2: エントリー基準厳格化（Phase 54.3-Aは逆効果で取消）
      ATRBased: 0.30            # = 0.30（PF 0.96）
      BBReversal: 0.50          # = 0.50（PF 1.92）
      DonchianChannel: 0.20     # = 0.20（PF 0.89）
      StochasticReversal: 0.0   # = 0.0（PF 0.02・無効化）
      ADXTrendStrength: 0.0     # = 0.0（トレンド専用・無効化）
      MACDEMACrossover: 0.0     # = 0.0（トレンド専用・無効化）
    normal_range:  # 通常レンジ相場（BB幅 < 5%, ADX < 20）
      # Phase 51.8-10: バランス型70/20配分（理論的初期値）
      ATRBased: 0.35            # ↑ 0.30→0.35（主力強化）
      BBReversal: 0.20          # = 0.20（維持）
      DonchianChannel: 0.10     # ↓ 0.15→0.10（削減）
      StochasticReversal: 0.15  # = 0.15（維持）
      ADXTrendStrength: 0.15    # = 0.15（弱トレンド検出）
      MACDEMACrossover: 0.05    # = 0.05（補助維持）
    trending:  # トレンド相場（ADX > 25, EMA傾き > 1%）
      # Phase 51.8-10: トレンド型80/20配分（理論的初期値）
      ADXTrendStrength: 0.50    # ↑ 0.40→0.50（トレンド主力強化）
      MACDEMACrossover: 0.30    # = 0.30（維持）
      ATRBased: 0.15            # ↓ 0.20→0.15（削減）
      DonchianChannel: 0.05     # ↓ 0.10→0.05（削減）
      BBReversal: 0.0           # = 0.0（無効化）
      StochasticReversal: 0.0   # = 0.0（無効化）
    high_volatility:  # 高ボラティリティ（ATR比 > 1.8%・4時間足）
      # 全戦略無効化（完全待機モード）
      ATRBased: 0.0
      BBReversal: 0.0
      DonchianChannel: 0.0
      StochasticReversal: 0.0
      ADXTrendStrength: 0.0
      MACDEMACrossover: 0.0

# ========================================
# 📊 レジーム別ポジション制限設定（Regime-Based Position Limits）
# ========================================
# Phase 51.8: レジーム別保有ポジション制限実装
#
# 目的: 市場レジームに応じて保有ポジション数を動的に制限し、
#       リスク管理を強化・市場状況に最適化されたポジション管理を実現
#
# 設定方法:
#   - レジーム名: tight_range, normal_range, trending, high_volatility
#   - max_positions: 最大同時保有ポジション数（0-5件）
#   - 制限超過時: 新規エントリー拒否（reject_on_limit: true）
#
# 設計根拠:
#   - tight_range: レンジ相場は逆張りチャンス多数 → 分散投資重視（5件）
#   - normal_range: バランス型 → 適度な分散（4件）
#   - trending: トレンド相場は集中投資 → 中程度の集中（3件）
#   - high_volatility: 完全待機 → 損失回避（0件）
# ========================================
position_limits:
  tight_range:
    max_positions: 6      # Phase 51.8-10: 5→6（分散投資最大化）
  normal_range:
    max_positions: 4      # バランス型（維持）
  trending:
    max_positions: 2      # Phase 51.8-10: 3→2（集中投資・損失最小化）
  high_volatility:
    max_positions: 0      # 完全待機（維持）
  # エラーフォールバック設定
  fallback_max_positions: 2    # レジーム情報取得失敗時のデフォルト値
  reject_on_limit: true         # 制限超過時は新規エントリー拒否

# ========================================
# 🎯 戦略パラメータ（Strategy Parameters）
# ========================================
# 5戦略の閾値・信頼度計算設定
# ========================================
strategies:
  adx_trend:
    di_crossover_threshold: 0.5
    di_weak_signal_confidence: 0.35
    hold_confidence: 0.45
    min_confidence: 0.25
    moderate_trend_max: 25
    moderate_trend_min: 15
    strong_trend_threshold: 25
    weak_di_threshold: 1.0
    weak_trend_threshold: 15
  atr_based:
    # Phase 54.2: エントリー基準厳格化（Phase 54.3-Bで過剰厳格化→戻す）
    bb_overbought: 0.85       # = 0.85（Phase 54.2維持・0.90は過剰）
    bb_oversold: 0.15         # = 0.15（Phase 54.2維持・0.10は過剰）
    high_volatility_strength: 0.5
    hold_confidence: 0.15
    low_volatility_strength: 0.2
    min_confidence: 0.35      # = 0.35（Phase 54.2維持・0.40は過剰）
    normal_volatility_strength: 0.25
    rsi_overbought: 70        # = 70（Phase 54.2維持・75は過剰）
    rsi_oversold: 30          # = 30（Phase 54.2維持・25は過剰）
    strength_fallback: 0.3
    base_confidence: 0.25
    confidence_multiplier: 0.3
    weak_signal_enabled: false  # Phase 54.2: 微弱シグナル無効化
  donchian_channel:
    # Phase 54.3-B: エントリー基準厳格化（PF 0.89→1.0+目標）
    breakout_threshold: 0.002
    hold_confidence: 0.4
    middle_zone_max: 0.6
    middle_zone_min: 0.4
    min_confidence: 0.40           # ↑ 0.35→0.40（最低信頼度引き上げ）
    reversal_threshold: 0.03       # ↓ 0.05→0.03（リバーサル厳格化）
    weak_signal_confidence: 0.35
    weak_signal_max: 0.75
    weak_signal_min: 0.25
    weak_signal_enabled: false     # Phase 54.2: 弱シグナル無効化
  bb_reversal:
    # Phase 51.7 Day 3: BB Reversal戦略設定
    # Phase 54.3-A結果: 閾値緩和は逆効果（PF 1.92→1.12）→オリジナル維持
    min_confidence: 0.30
    hold_confidence: 0.25
    bb_width_threshold: 0.02        # BB幅閾値（2% = レンジ相場判定）
    rsi_overbought: 70              # RSI買われすぎ閾値
    rsi_oversold: 30                # RSI売られすぎ閾値
    bb_upper_threshold: 0.95        # BB上限位置閾値（厳格維持）
    bb_lower_threshold: 0.05        # BB下限位置閾値（厳格維持）
    adx_range_threshold: 20         # ADXレンジ相場閾値（20未満）
    sl_multiplier: 1.5              # SL ATR倍率（レンジ相場用）
  stochastic_reversal:
    # Phase 51.7 Day 4: Stochastic Reversal戦略設定
    min_confidence: 0.30
    hold_confidence: 0.25
    stoch_overbought: 80            # Stochastic過買い閾値
    stoch_oversold: 20              # Stochastic過売り閾値
    rsi_overbought: 65              # RSI買われすぎ閾値
    rsi_oversold: 35                # RSI売られすぎ閾値
    adx_range_threshold: 20         # ADXレンジ相場閾値（20未満）
    sl_multiplier: 1.5              # SL ATR倍率（レンジ相場用）
  macd_ema_crossover:
    # Phase 51.7 Day 5: MACD + EMA Crossover戦略設定
    min_confidence: 0.35
    hold_confidence: 0.25
    adx_trend_threshold: 25         # ADXトレンド相場閾値（25以上）
    volume_ratio_threshold: 1.1     # 出来高増加閾値
    macd_strong_threshold: 50000    # MACD強い閾値（ヒストグラム正規化用）
    ema_divergence_threshold: 0.01  # EMA乖離度閾値（1%）
    sl_multiplier: 1.5              # SL ATR倍率（トレンド相場用）

# ========================================
# 💹 取引設定（Trading Settings）
# ========================================
# 取引実行・信頼度レベル・Kelly基準設定
# ========================================
trading:
  anomaly:
    spread_critical_threshold: 0.005
    spread_warning_threshold: 0.003
  ask_spread_ratio: 1.001
  bid_spread_ratio: 0.999
  confidence_levels:
    high: 0.45
    low: 0.2
    medium: 0.3
    min_ml: 0.12
    very_high: 0.6
  fallback_btc_jpy: 16500000.0
  fallback_prices:
    ask: 16600000.0
    bid: 16400000.0
    default_ask: 16100000.0
    default_bid: 15900000.0
  kelly_criterion:
    min_trades_for_kelly: 5
    max_position_ratio: 0.03
    safety_factor: 0.7
  min_trade_size: 0.0001
  initial_position_size: 0.0001
  fee_rate: 0.0012
  minimum_live_balance: 3000
  position_sizing:
    max_position_ratio: 0.05
    stop_loss_atr_multiplier: 1.2
    stop_loss_rate: 0.02
    take_profit_rate: 0.04
    take_profit_ratio: 2.0
  risk_thresholds:
    conditional: 0.65
    deny: 0.85
    min_ml_confidence: 0.12
  stats_log_interval: 10

# ========================================
# 🧠 アンサンブルモデル設定（Ensemble Model Settings）
# ========================================
ensemble:
  max_nan_ratio_features: 0.5
  max_nan_ratio_target: 0.3
  min_training_samples: 50

# ========================================
# 📈 MLモデル設定（ML Model Settings）
# ========================================
# LightGBM・XGBoost・RandomForestハイパーパラメータ
# ========================================
models:
  lgbm:
    num_leaves: 31
    learning_rate: 0.05
    n_estimators: 100
    feature_fraction: 0.9
    bagging_fraction: 0.8
    bagging_freq: 5
    max_depth: -1
    min_child_samples: 20
    reg_alpha: 0.0
    reg_lambda: 0.0
  min_training_samples: 10
  rf:
    n_estimators: 100
    max_depth: 10
    min_samples_split: 5
    min_samples_leaf: 2
    max_features: sqrt
    bootstrap: true
    oob_score: true
    class_weight: balanced
  xgb:
    max_depth: 6
    learning_rate: 0.05
    n_estimators: 100
    min_child_weight: 1
    subsample: 0.8
    colsample_bytree: 0.8
    alpha: 0
    lambda: 1
    gamma: 0

# ========================================
# 📊 レポート設定（Reporting Settings）
# ========================================
reporting:
  base_dir: logs/reports
  paper_trading_dir: logs/paper_trading_reports

# ========================================
# ⚖️ リスク管理設定（Risk Management Settings）
# ========================================
# Kelly基準・ドローダウン・資金管理設定
# Phase 51.5-C: TP/SL価格差異バグ修正（ticker価格 vs limit価格・ATR取得失敗対応）
# ========================================
risk:
  emergency_ratio: 0.005
  emergency_stop_ratio: 0.98
  fallback_max_ratio: 0.1
  fallback_min_ratio: 0.01
  fallback_stop_ratio: 0.95
  kelly_lookback_days: 30
  recent_lookback_hours: 24
  safe_balance_ratio: 0.3
  max_capital_usage: 0.3
  reserve_ratio: 0.7

  # Phase 51.5-C: TP/SL再計算フォールバック設定
  fallback_atr: 500000                   # ATR取得失敗時のフォールバックATR値（500,000円）
  require_tpsl_recalculation: true       # TP/SL再計算必須化（false時は元のTP/SL使用）

# ========================================
# 📈 ポジション管理設定（Position Management Settings）
# ========================================
# Phase 51.6完了（被害最小化・こまめ利確）:
#   - SL 0.7%・TP 0.9%・RR比1.29:1（Phase 51.6: 被害最小化・こまめ利確）
#   - Phase 46: 個別TP/SL実装（デイトレード特化・シンプル実装）
#   - Phase 49.18: 証拠金維持率80%確実遵守（critical: 100.0 → 80.0変更）
#   - Phase 50.5: SL注文保護（クリーンアップ機能一時無効化）
# ========================================
position_management:
  # Phase 46: 個別TP/SL実装（Phase 42統合TP/SL削除済み）
  min_account_balance: 10000
  max_open_positions: 3
  max_daily_trades: 50
  cooldown_minutes: 15
  min_trade_size: 0.0001
  max_position_ratio_per_trade:
    low_confidence: 0.03
    medium_confidence: 0.05
    high_confidence: 0.1
    enforce_minimum: true
  dynamic_position_sizing:
    enabled: true
    low_confidence:
      min_ratio: 0.01
      max_ratio: 0.03
    medium_confidence:
      min_ratio: 0.03
      max_ratio: 0.05
    high_confidence:
      min_ratio: 0.05
      max_ratio: 0.1
  account_size_adjustments:
    small:
      threshold: 50000
      min_trade_override: true
    medium:
      threshold: 100000
      position_multiplier: 1.0
    large:
      threshold: 100001
      position_multiplier: 1.0
  emergency_stop_loss:
    enable: true
    price_change_threshold: 0.03
    time_window_minutes: 5
    max_loss_threshold: 0.05
    override_cooldown: true
    min_hold_minutes: 1
  take_profit:
    enabled: true
    default_ratio: 1.29              # Phase 51.6: RR比1.29:1（TP 0.9% / SL 0.7%）
    min_profit_ratio: 0.009          # Phase 51.6: TP 0.9%（こまめ利確・maker手数料込み1.1%利益）
    override_strategy_tp: false
    position_partial_exit: false

    # ========================================
    # Phase 52.0: レジーム別TP設定
    # ========================================
    # 目的: 市場レジームに応じたTP最適化
    #   - tight_range: TP 0.8%（頻繁利確・93.9%発生率対応）
    #   - normal_range: TP 1.0%（標準利確）
    #   - trending: TP 1.5%（大きく伸ばす）
    # 期待効果: 勝率46.1%→50%+、取引毎リターン+219%
    # ========================================
    regime_based:
      enabled: true
      tight_range:
        min_profit_ratio: 0.008      # TP 0.8%（RR比1.33:1）
        default_ratio: 1.33
      normal_range:
        min_profit_ratio: 0.010      # TP 1.0%（RR比1.43:1）
        default_ratio: 1.43
      trending:
        min_profit_ratio: 0.015      # TP 1.5%（RR比1.50:1）
        default_ratio: 1.50
      high_volatility:
        min_profit_ratio: 0.020      # TP 2.0%（参考値・実際はエントリーなし）
        default_ratio: 1.33
  stop_loss:
    enabled: true
    default_atr_multiplier: 2.0
    max_loss_ratio: 0.007            # Phase 51.6: SL 0.7%（被害最小化・1回約11円損失）
    override_strategy_sl: false
    enable_trailing: false
    execution_slippage: 0.003

    # ========================================
    # Phase 52.0: レジーム別SL設定
    # ========================================
    # 目的: 市場レジームに応じたSL最適化
    #   - tight_range: SL 0.6%（損失最小化・レンジ相場特化）
    #   - normal_range: SL 0.7%（標準損切り）
    #   - trending: SL 1.0%（トレンド継続余地確保）
    # 期待効果: 平均損失-12円→-10円（-17%改善）
    # ========================================
    regime_based:
      enabled: true
      tight_range:
        max_loss_ratio: 0.006        # SL 0.6%（RR比1.33:1）
      normal_range:
        max_loss_ratio: 0.007        # SL 0.7%（RR比1.43:1）
      trending:
        max_loss_ratio: 0.010        # SL 1.0%（RR比1.50:1）
      high_volatility:
        max_loss_ratio: 0.015        # SL 1.5%（参考値・実際はエントリーなし）

    adaptive_atr:
      enabled: true
      use_15m_timeframe: true
      volatility_lookback: 20
      low_volatility:
        threshold_ratio: 0.7
        multiplier: 2.5
      normal_volatility:
        multiplier: 2.0
      high_volatility:
        threshold_ratio: 1.3
        multiplier: 1.5
    min_distance:
      enabled: true
      ratio: 0.007               # Phase 51.6: SL 0.7%に合わせて変更
      override_atr: false
      min_atr_multiplier: 1.5
    trailing:
      enabled: false
      activation_profit: 0.02
      trailing_percent: 0.03
      min_update_distance: 200
      min_profit_lock: 0.005
      cancel_tp_when_exceeds: true

# ========================================
# 💰 証拠金管理設定（Margin Management Settings）
# ========================================
# Phase 49.18完了: 維持率80%確実遵守（critical: 100.0 → 80.0変更）
# ========================================
margin:
  position_value_estimation_ratio: 0.8
  # 維持率閾値（80%確実遵守）
  thresholds:
    critical: 80.0
    warning: 100.0
    caution: 150.0
    safe: 200.0
  large_drop_threshold: 50.0
  min_position_value: 1000.0
  max_ratio_cap: 10000.0

# ========================================
# 📝 注文実行設定（Order Execution Settings）
# ========================================
# Phase 38.7.2完了: 完全指値オンリー実装（100%指値注文）
# ========================================
order_execution:
  smart_order_enabled: true
  high_confidence_threshold: 0.0
  low_confidence_threshold: -1.0
  max_spread_ratio_for_limit: 0.005
  price_improvement_ratio: 0.0002
  entry_price_strategy: unfavorable
  guaranteed_execution_premium: 0.0005
  limit_order_timeout:
    enabled: true
    timeout_minutes: 5
    fallback_to_market: true
    max_price_deviation: 0.05
    check_interval_seconds: 60

# ========================================
# 🔔 残高アラート設定（Balance Alert Settings）
# ========================================
balance_alert:
  enabled: true
  min_required_margin: 14000.0
  alert_threshold_ratio: 1.2
  check_interval_minutes: 3
  discord_critical_alert: true

# ==================================================================================
# 🔬 Optuna最適化パラメータ（Phase 40最適化結果）
# ==================================================================================
# 目的: Phase 40.1-40.4で最適化された79パラメータの最適値
#
# 重要:
#   1. 上記セクションのパラメータと一部重複しています
#   2. システムは上記セクションの値を優先使用します（get_threshold()参照）
#   3. このセクションは最適化結果の記録・将来的な参照用です
#
# 最適化対象:
#   - Phase 40.1: リスク管理パラメータ（12個）
#   - Phase 40.2: 戦略パラメータ（30個）
#   - Phase 40.3: ML統合パラメータ（7個）
#   - Phase 40.4: MLハイパーパラメータ（30個）
#
# 更新方法:
#   scripts/optimization/run_phase40_optimization.py を使用
#   Phase 40.5統合デプロイで自動更新されます
#
# 期待効果:
#   +50-70%の収益向上（リスク+10-15%・戦略+15-20%・ML統合+10-15%・MLハイパー+15-20%）
# ==================================================================================
donchian_breakout_base: 0.5
donchian_reversal_base: 0.4
donchian_weak_base: 0.25
donchian_breakout_threshold: 0.0015
donchian_reversal_threshold: 0.09
adx_strong_base: 0.7000000000000001
adx_moderate_base: 0.45
adx_weak_base: 0.3
adx_strong_threshold: 23
adx_moderate_min: 10
adx_di_crossover: 0.6000000000000001
adx_di_confirmation: 0.4
atr_high_vol_base: 0.55
atr_normal_vol_base: 0.5
atr_low_vol_base: 0.4
atr_rsi_overbought: 63
atr_rsi_oversold: 26
atr_bb_overbought: 0.8
atr_bb_oversold: 0.35
ml_weight: 0.35
high_confidence_threshold: 0.85
agreement_bonus: 1.15
disagreement_penalty: 0.85
min_ml_confidence: 0.45
hold_conversion_threshold: 0.4
lgbm_num_leaves: 40
lgbm_learning_rate: 0.03842362394715147
lgbm_n_estimators: 200
lgbm_max_depth: 10
lgbm_max_depth_unlimited: true
lgbm_min_child_samples: 100
lgbm_feature_fraction: 0.65
lgbm_bagging_fraction: 0.8
lgbm_bagging_freq: 7
lgbm_reg_alpha: 4.9643553965532245e-05
lgbm_reg_lambda: 0.05302953797331255
xgb_max_depth: 8
xgb_learning_rate: 0.09229165545667654
xgb_n_estimators: 350
xgb_min_child_weight: 7
xgb_subsample: 0.9
xgb_colsample_bytree: 0.8
xgb_gamma: 4.5
xgb_alpha: 0.006691186940196909
xgb_lambda: 0.20681062974015182
xgb_scale_pos_weight: 0.9
rf_n_estimators: 200
rf_max_depth: 24
rf_max_depth_none: false
rf_min_samples_split: 18
rf_min_samples_leaf: 5
rf_max_features_type: sqrt
rf_max_features_float: 0.5
rf_max_leaf_nodes: 50
rf_max_leaf_nodes_none: true
rf_min_impurity_decrease: 0.06
rf_bootstrap: true
rf_oob_score: false
rf_class_weight: null
