# ──────────────────────────────────────────────────────────────────────────────
# Claude Code runtime files (local-only)
# ──────────────────────────────────────────────────────────────────────────────
.claude/

# ──────────────────────────────────────────────────────────────────────────────
# Python bytecode（.cache/pycache に統合済み）
# ──────────────────────────────────────────────────────────────────────────────
__pycache__/          # 旧形式（PYTHONPYCACHEPREFIX で .cache/pycache に統合済み）
*.py[cod]
*.pyo
*$py.class
*.so

# ──────────────────────────────────────────────────────────────────────────────
# ビルド成果物 / パッケージング
# ──────────────────────────────────────────────────────────────────────────────
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
pip-wheel-metadata/

# ──────────────────────────────────────────────────────────────────────────────
# 統合キャッシュディレクトリ（全て .cache 配下に集約）
# ──────────────────────────────────────────────────────────────────────────────
.cache/

# 旧キャッシュ系（統合前の散らばりファイル対策）
.pytest_cache/
.mypy_cache/
.pytype/
.coverage_data/
test_reports/

# カバレッジレポート（.cache/coverage/で管理）
.coverage                        # ルートの一時カバレッジファイル
coverage.xml                     # XML形式レポート

# ──────────────────────────────────────────────────────────────────────────────
# 仮想環境
# ──────────────────────────────────────────────────────────────────────────────
venv/
env/
ENV/
.venv/
.env/
__pypackages__/   # PEP 582

# ──────────────────────────────────────────────────────────────────────────────
# 環境変数・設定ファイル
# ──────────────────────────────────────────────────────────────────────────────
.env
.env.*            # .env.local など
config/local.yml
config/production.yml

# Discord Webhook設定（機密情報）
config/secrets/

# GCP IAMポリシーバックアップ（機密情報・自動生成）
config/infrastructure/iam_policy_backup.json

# ──────────────────────────────────────────────────────────────────────────────
# IDE・エディタ
# ──────────────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.code-workspace
*.swp
*.swo
*~

# ──────────────────────────────────────────────────────────────────────────────
# OS関連
# ──────────────────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ──────────────────────────────────────────────────────────────────────────────
# ログ / 結果 / モデルなど実行時生成物
# ──────────────────────────────────────────────────────────────────────────────
*.log
logs/*
!logs/README.md
results/
*/results/

# 分析結果（自動生成・Git管理外）
docs/検証記録/live/
docs/検証記録/analysis_*.json
docs/検証記録/analysis_*.md
docs/検証記録/analysis_history.csv

# ──────────────────────────────────────────────────────────────────────────────
# 学習済みモデル・ML関連
# ──────────────────────────────────────────────────────────────────────────────
# 動的生成される学習中間ファイル・一時モデル
*.pkl
# 本番用 2 段階モデルは例外（Phase 90α: 55 特徴量・4 モデル）
!models/production/ensemble_full.pkl
!models/production/ensemble_basic.pkl
*.model
*.joblib
*.h5
*.pt
*.pth
*.onnx

# 学習ログ・実験結果
mlruns/
wandb/
tensorboard_logs/
training_logs/
model_artifacts/

# ただし、管理対象のモデルファイルは保持
!models/archive/                  # モデルアーカイブディレクトリ
!models/**/*.pkl                  # 本番・学習用モデル

# モデル評価・実験の一時結果
model_evaluation_*.json
experiment_*.csv
hyperparameter_*.json

# ──────────────────────────────────────────────────────────────────────────────
# データファイル
# ──────────────────────────────────────────────────────────────────────────────
# 動的データ・ダウンロードファイル
raw_data/
downloaded_data/
cache_data/
*.csv
*.parquet
*.feather
*.hdf5

# バックテスト関連データ（動的生成）
src/backtest/data/*.csv           # 履歴データキャッシュ
src/backtest/data/*.json          # 結果データ
src/backtest/logs/                # 旧出力先（Phase 64.11で logs/backtest/ に移行）
backtest_results/
historical_data/

# ただし、ソースコード・設定ファイルは保持
!src/data/                        # src/data/ ディレクトリ自体は保持
!src/data/*.py                    # データ処理ソースコード
!src/backtest/data/README.md      # バックテストデータ説明
!config/**/*.csv                  # 設定用CSVファイル

# ──────────────────────────────────────────────────────────────────────────────
# 実行時生成ファイル
# ──────────────────────────────────────────────────────────────────────────────
# ランタイムデータ（孤児SL追跡等）
data/*
!data/README.md
!data/orderbook/
data/orderbook/*
!data/orderbook/.gitkeep

# 取引・リスク管理関連
drawdown_state.json
risk_state.json
trading_state.json
position_*.json

# 監視・ログ関連
status.json
health_check.json
*.monitoring_*.log
pipeline_log/
performance_*.json

# 分析・レポート関連（scripts/analysis/）
logs/performance_analysis/
logs/operational_reports/
logs/monitoring_reports/
analysis_report_*.md
analysis_report_*.json

# ──────────────────────────────────────────────────────────────────────────────
# バックアップ・一時ファイル
# ──────────────────────────────────────────────────────────────────────────────
*.tmp
*.temp
temp/
tmp/
*.bak
*.backup
*.json.bak
*.yaml.bak
backups/
feature_order_backup_*.json

# ──────────────────────────────────────────────────────────────────────────────
# 一時的な分析・開発ファイル
# ──────────────────────────────────────────────────────────────────────────────
analyze_results.py
analysis_script.py
big.txt
temp_*.py
*_temp.py

# 投機・試算分析ファイル
aggressive_projection_analysis.py
analyze_bitbank_projection.py
realistic_annual_projection.py
realistic_bitbank_projection.py

# ──────────────────────────────────────────────────────────────────────────────
# Docker関連
# ──────────────────────────────────────────────────────────────────────────────
# Phase 88 L3: .dockerignore はリポジトリで管理する（Cloud Run イメージ最適化用）
# 旧来の *.dockerignore 一括除外は廃止
docker-compose.override.yml

# ──────────────────────────────────────────────────────────────────────────────
# Terraform
# ──────────────────────────────────────────────────────────────────────────────
**/.terraform/
**/*.tfstate
**/*.tfstate.backup
**/.terraform.lock.hcl
.terraform.tfstate.lock.info
# 注意: 旧 `archive/` パターン除外は models/archive/ を誤マッチさせるため削除（Phase 90α）

# ──────────────────────────────────────────────────────────────────────────────
# レガシーパターン（過去のレガシーシステム名残・将来の誤参照防止）
# ──────────────────────────────────────────────────────────────────────────────
# _legacy_v1/ は Phase XX で完全削除済（実体無し・将来再作成された場合の保険）
_legacy_v1/

# レガシーシステム関連の一時ファイル・生成物
**/legacy_*
**/old_*
**/backup_*
_old/
_backup/
legacy/data/
# Tax database files (exclude SQLite databases)
tax/*.db
tax/*.csv
tax/*.txt

# Optimization checkpoints (temporary files)
config/optimization/checkpoints/
