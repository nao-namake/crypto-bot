# CI/CDパイプライン
# Phase 52.2整理（55特徴量・Python 3.13・Phase 49ベース）

name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
  repository_dispatch:
    types: [model-updated]

env:
  PROJECT_ID: my-crypto-bot-project
  REGION: asia-northeast1
  REPOSITORY: crypto-bot-repo
  SERVICE_NAME: crypto-bot-service-prod  # 本番サービス名（統一）

jobs:
  # ========================================
  # 品質チェック（checks.sh統合版）
  # ========================================
  
  quality-check:
    name: Quality Check (checks.sh)
    runs-on: ubuntu-24.04  # ランナー取得問題対策
    timeout-minutes: 15  # Phase 55.8: MLフル検証追加のため延長

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: 'pip'
          cache-dependency-path: 'requirements.txt'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Setup test environment
        run: |
          # Phase 52.2: 実モデルファイルがGitに含まれるため、ダミー作成不要
          mkdir -p logs cache
          echo '{"version": "ci-test", "features": 55}' > models/production/model_metadata.json
          echo "✅ テスト環境準備完了（実モデルファイル使用）"

      - name: Collect historical data for ML validation
        run: |
          # Phase 55.8: MLフル検証用データ収集（7日分・軽量）
          echo "📥 ML検証用履歴データ収集開始（7日分）..."
          mkdir -p src/backtest/data/historical

          python3 src/backtest/scripts/collect_historical_csv.py --days 7

          # データ収集確認
          if [ -f "src/backtest/data/historical/BTC_JPY_15m.csv" ]; then
            CSV_LINES=$(wc -l < src/backtest/data/historical/BTC_JPY_15m.csv)
            echo "✅ 15分足データ収集完了: ${CSV_LINES}行"
            # 小文字ファイル名にもコピー（検証スクリプト互換性）
            cp src/backtest/data/historical/BTC_JPY_15m.csv src/backtest/data/historical/btc_jpy_15m.csv
          else
            echo "⚠️ 15分足データ収集失敗（検証スキップ）"
          fi

      - name: Run quality checks (checks.sh)
        run: bash scripts/testing/checks.sh

      - name: ML Model Validation (Full)
        run: |
          # Phase 55.8: MLモデルフル検証（予測分布・HOLD率チェック含む）
          echo "🤖 Phase 55.8: MLモデルフル検証開始"
          python3 scripts/testing/validate_ml_models.py || {
            echo "❌ MLモデル検証失敗"
            echo "→ HOLD率が90%以上の場合、モデル再訓練が必要です"
            echo "→ コマンド: python scripts/ml/create_ml_models.py --threshold 0.0005 --n-classes 3 --use-smote --optimize --n-trials 30"
            exit 1
          }
          echo "✅ MLモデルフル検証完了"

  # ========================================
  # GCP環境検証（Phase 16-B対応）
  # ========================================
  gcp-environment-check:
    name: GCP Environment Verification
    needs: quality-check
    runs-on: ubuntu-24.04  # ランナー取得問題対策
    timeout-minutes: 10
    
    # mainブランチプッシュ時のみ実行（デプロイ前検証）
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/11445303925/locations/global/workloadIdentityPools/github-pool/providers/github-provider
          service_account: github-deployer@my-crypto-bot-project.iam.gserviceaccount.com
          access_token_lifetime: 1800s

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Run GCP Environment Pre-Check
        run: |
          echo "🔍 統一設定管理体系: GCP環境事前検証開始"
          echo "デプロイ前にGCP環境の準備状況を確認します"

          # レガシー方式：認証確認強化版（権限反映ラグ対応）
          echo "🔍 レガシー方式：軽量GCP検証実行"
          
          # 1. 認証状態詳細確認（Service Account偽装確認）
          echo "🔐 認証状態確認..."
          ACTIVE_ACCOUNT=$(gcloud auth list --filter=status:ACTIVE --format="value(account)" | head -n1)
          if [ -n "$ACTIVE_ACCOUNT" ]; then
            echo "✅ gcloud認証確認: $ACTIVE_ACCOUNT"
            # Service Account偽装確認
            if [[ "$ACTIVE_ACCOUNT" == *"github-deployer@my-crypto-bot-project.iam.gserviceaccount.com"* ]]; then
              echo "✅ Service Account偽装成功: github-deployer"
            else
              echo "⚠️ Service Account偽装未確認: $ACTIVE_ACCOUNT"
              echo "デバッグ: Workload Identity Poolから Service Account への偽装確認中..."
            fi
          else
            echo "❌ gcloud認証失敗"
            exit 1
          fi
          
          # 2. プロジェクト設定確実化
          echo "🏗️ プロジェクト設定確実化..."
          if gcloud config set project ${{ env.PROJECT_ID }}; then
            # 設定確認
            CURRENT_PROJECT=$(gcloud config get-value project 2>/dev/null)
            if [ "$CURRENT_PROJECT" = "${{ env.PROJECT_ID }}" ]; then
              echo "✅ プロジェクト設定確認: ${{ env.PROJECT_ID }}"
            else
              echo "❌ プロジェクト設定不一致: expected=${{ env.PROJECT_ID }}, actual=$CURRENT_PROJECT"
              exit 1
            fi
          else
            echo "❌ プロジェクト設定失敗: ${{ env.PROJECT_ID }}"
            exit 1
          fi
          
          # 3. 権限反映待機（認証ラグ対応）
          echo "⏳ 権限反映待機（3秒）..."
          sleep 3
          
          echo "✅ GCP環境基本確認完了 - リソース確認へ移行"

      - name: Verify Critical GCP Resources
        run: |
          echo "🏗️ 重要なGCPリソース確認 - 必須リソース確認強化（権限反映ラグ対応版）"
          
          # 認証状態再確認
          echo "🔐 認証状態再確認..."
          CURRENT_PROJECT=$(gcloud config get-value project 2>/dev/null)
          if [ "$CURRENT_PROJECT" != "${{ env.PROJECT_ID }}" ]; then
            echo "❌ プロジェクト設定異常: $CURRENT_PROJECT"
            exit 1
          fi
          echo "✅ プロジェクト設定正常: $CURRENT_PROJECT"
          
          # Artifact Registry確認（必須・エラーハンドリング強化）
          echo "📦 Artifact Registry確認..."
          if gcloud artifacts repositories describe ${{ env.REPOSITORY }} \
              --location=${{ env.REGION }} \
              --project=${{ env.PROJECT_ID }} 2>/tmp/artifact_error.log; then
            echo "✅ Artifact Registry準備完了"
          else
            echo "❌ Artifact Registry確認失敗"
            echo "エラー詳細:"
            cat /tmp/artifact_error.log
            echo ""
            echo "デバッグ情報:"
            echo "- プロジェクト: ${{ env.PROJECT_ID }}"
            echo "- リージョン: ${{ env.REGION }}"
            echo "- リポジトリ: ${{ env.REPOSITORY }}"
            echo "- 認証アカウント: $(gcloud auth list --filter=status:ACTIVE --format='value(account)' | head -n1)"
            exit 1
          fi
          
          # Secret Manager確認（必須・詳細エラー）
          echo "🔐 Secret Manager確認..."
          required_secrets=("bitbank-api-key" "bitbank-api-secret")
          missing_secrets=()
          
          for secret in "${required_secrets[@]}"; do
            if gcloud secrets describe "$secret" --project=${{ env.PROJECT_ID }} 2>/tmp/secret_error_${secret}.log; then
              echo "✅ Secret設定済み: $secret"
            else
              echo "❌ Secret未設定: $secret"
              echo "エラー詳細:"
              cat /tmp/secret_error_${secret}.log
              missing_secrets+=("$secret")
            fi
          done
          
          if [ ${#missing_secrets[@]} -eq 0 ]; then
            echo "✅ 必須Secret Manager設定完了"
          else
            echo "❌ 必須Secret未設定 - デプロイ不可"
            echo "未設定Secret: ${missing_secrets[*]}"
            exit 1
          fi
          
          echo "🎯 統一設定管理体系: 全必須リソース確認完了 - デプロイ実行準備完了"

  # ========================================
  # 3. Docker Build & GCP Deploy（本番用）
  # ========================================
  build-deploy:
    name: Build & Deploy to GCP
    needs: [quality-check, gcp-environment-check]
    runs-on: ubuntu-24.04  # ランナー取得問題対策
    timeout-minutes: 30
    
    # mainブランチプッシュ時のみ実行
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create required directories for Docker
        run: |
          # Docker用の必要なディレクトリ作成
          mkdir -p models/production logs cache
          echo '{"version": "production", "features": 55, "created": "'$(date -u +%Y-%m-%dT%H:%M:%SZ)'"}' > models/production/model_metadata.json
          
          # Phase 50.9: 本番用モデル確認（CI環境では作成済み）
          if [ -f "models/production/ensemble_full.pkl" ]; then
            echo "✅ 本番用モデル確認済み: models/production/ensemble_full.pkl"
          else
            echo "⚠️ 本番用モデル未確認（CI環境で作成予定）"
          fi

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/11445303925/locations/global/workloadIdentityPools/github-pool/providers/github-provider
          service_account: github-deployer@my-crypto-bot-project.iam.gserviceaccount.com
          access_token_lifetime: 3600s

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev

      - name: Verify Artifact Registry repository exists
        run: |
          # レポジトリ存在確認（必須）
          echo "🔍 Artifact Registryレポジトリ存在確認..."
          if gcloud artifacts repositories describe ${{ env.REPOSITORY }} \
              --location=${{ env.REGION }} \
              --project=${{ env.PROJECT_ID }} >/dev/null 2>&1; then
            echo "✅ Artifact Registryレポジトリ確認済み"
          else
            echo "❌ Artifact Registryレポジトリが見つかりません - デプロイ不可"
            echo "必要な作成コマンド："
            echo "gcloud artifacts repositories create ${{ env.REPOSITORY }} --repository-format=docker --location=${{ env.REGION }} --project=${{ env.PROJECT_ID }}"
            exit 1
          fi

      - name: Build and Push Docker Image
        run: |
          # イメージタグ設定
          IMAGE_TAG="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/crypto-bot"
          COMMIT_SHA="${{ github.sha }}"
          
          echo "🐳 Dockerイメージビルド開始: ${IMAGE_TAG}:${COMMIT_SHA}"
          
          # Dockerビルド（Phase 7最適化版使用）
          docker build -t "${IMAGE_TAG}:${COMMIT_SHA}" -t "${IMAGE_TAG}:latest" .
          
          # プッシュ
          docker push "${IMAGE_TAG}:${COMMIT_SHA}"
          docker push "${IMAGE_TAG}:latest"
          
          echo "✅ Dockerイメージプッシュ完了"

      - name: Docker Container Startup Test (Phase 49.14)
        run: |
          # Phase 49.14: Dockerコンテナ起動テスト（import検証）
          IMAGE_TAG="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/crypto-bot:${{ github.sha }}"

          echo "🐳 Phase 49.14: Docker Container起動テスト実行中..."
          echo "イメージ: ${IMAGE_TAG}"

          # コンテナ起動テスト（Python import検証）
          # Phase 49.13のような 'No module named tax' エラーを事前検出
          if docker run --rm "${IMAGE_TAG}" python3 -c "
          import sys
          sys.path.insert(0, '/app')
          sys.path.insert(0, '/app/src')

          # 重要モジュールimport検証
          from src.core.orchestration.orchestrator import TradingOrchestrator
          from src.trading.execution.executor import ExecutionService
          from tax.trade_history_recorder import TradeHistoryRecorder
          from src.strategies.base.strategy_manager import StrategyManager

          print('✅ All critical imports successful')
          "; then
            echo "✅ Docker Container起動テスト成功 - 全モジュールimport可能"
          else
            echo "❌ Docker Container起動テスト失敗 - モジュールimportエラー"
            echo "Phase 49.13 'No module named ...' 問題を検出しました"
            echo "Dockerfileの COPY 命令を確認してください"
            exit 1
          fi

      - name: 段階的Cloud Runデプロイ（レガシー改良版）
        run: |
          IMAGE_TAG="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/crypto-bot:${{ github.sha }}"

          echo "🚀 段階的デプロイ開始: ${IMAGE_TAG}"
          echo "Phase 52.2: 55特徴量・Python 3.13"
          
          # デプロイモード判定（レガシー改良・本番モードデフォルト）
          DEPLOY_MODE="${{ secrets.DEPLOY_MODE || 'live' }}"
          echo "📊 デプロイモード: $DEPLOY_MODE"
          
          # Phase 16-B統一設定（本番モード固定・設定一元化対応）
          SERVICE_SUFFIX=""
          MEMORY="1Gi"
          CPU="1"
          MIN_INSTANCES="1"
          MAX_INSTANCES="2"
          TIMEOUT="3600"
          DEPLOY_MODE="live"
          echo "📋 本番モード（ライブトレード）"
          
          FULL_SERVICE_NAME="${{ env.SERVICE_NAME }}"
          echo "📍 デプロイサービス名: $FULL_SERVICE_NAME"
          
          # Cloud Runデプロイ実行（Phase 16-B段階的対応・設定一元化対応）
          echo "🔍 サービス存在確認: $FULL_SERVICE_NAME"
          if gcloud run services describe "$FULL_SERVICE_NAME" --region=${{ env.REGION }} >/dev/null 2>&1; then
            echo "✅ 既存サービス更新（--no-traffic使用可能）"
            TRAFFIC_FLAG="--no-traffic"
          else
            echo "🆕 新規サービス作成（--no-traffic不可）"
            TRAFFIC_FLAG=""
          fi
          
          gcloud run deploy "$FULL_SERVICE_NAME" \
            --image="${IMAGE_TAG}" \
            --region=${{ env.REGION }} \
            --platform=managed \
            --memory="$MEMORY" \
            --cpu="$CPU" \
            --min-instances="$MIN_INSTANCES" \
            --max-instances="$MAX_INSTANCES" \
            --concurrency=1 \
            --timeout="$TIMEOUT" \
            --allow-unauthenticated \
            --set-env-vars="MODE=$DEPLOY_MODE,LOG_LEVEL=INFO,PYTHONPATH=/app,FEATURE_MODE=full,DEPLOY_STAGE=$DEPLOY_MODE" \
            --set-secrets="BITBANK_API_KEY=bitbank-api-key:3,BITBANK_API_SECRET=bitbank-api-secret:3,DISCORD_WEBHOOK_URL=discord-webhook-url:6" \
            --port=8080 \
            --revision-suffix="unified-system-$(date +%m%d-%H%M)" \
            $TRAFFIC_FLAG
          
          # 本番トラフィック移行
          echo "🔄 100%本番トラフィック移行"
          gcloud run services update-traffic "$FULL_SERVICE_NAME" \
            --to-latest \
            --region=${{ env.REGION }} || echo "⚠️ トラフィック移行失敗"

          echo "✅ Phase 52.2本番デプロイ完了・システム稼働開始"

      - name: Verify Deployment Health Check
        run: |
          echo "🏥 デプロイ後ヘルスチェック実行"
          
          # 本番サービス名統一
          FULL_SERVICE_NAME="${{ env.SERVICE_NAME }}"
          echo "📍 ヘルスチェック対象: $FULL_SERVICE_NAME"
          
          # サービスURL取得
          SERVICE_URL=$(gcloud run services describe "$FULL_SERVICE_NAME" \
            --region=${{ env.REGION }} \
            --format='value(status.url)')
          
          echo "📍 サービスURL: ${SERVICE_URL}"
          
          # デプロイ状態確認（Cloud Run認証によりcurlは使用不可）
          echo "📊 デプロイ状態確認中..."

          # サービスステータス確認
          SERVICE_STATUS=$(gcloud run services describe "$FULL_SERVICE_NAME" \
            --region=${{ env.REGION }} \
            --format='value(status.conditions[0].status)')

          echo "📍 サービスステータス: ${SERVICE_STATUS}"

          if [ "$SERVICE_STATUS" = "True" ]; then
            echo "✅ Cloud Runサービス正常稼働中"

            # 起動ログ確認（最大5回リトライ）
            for i in {1..5}; do
              echo "🔍 起動ログ確認試行 $i/5"

              # 最新のログを確認（起動成功メッセージ）
              if gcloud logging read "resource.type=cloud_run_revision AND resource.labels.service_name=$FULL_SERVICE_NAME AND textPayload:\"ライブトレード起動完了\"" \
                --limit=1 --format="value(timestamp)" --freshness=5m | grep -q .; then
                echo "✅ ライブトレード起動ログ確認成功！"
                echo "🎉 デプロイ完了・サービス正常稼働中"
                exit 0
              else
                echo "⏳ 起動ログ未確認、30秒後にリトライ..."
                sleep 30
              fi
            done

            echo "⚠️ 起動ログ確認タイムアウト（サービスステータスは正常）"
            echo "📝 手動でログ確認を推奨: gcloud logging read"
            exit 0
          else
            echo "❌ Cloud Runサービス異常状態"
            exit 1
          fi

      - name: Post-deployment notification
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "🎉 Phase 52.2本番デプロイ成功！"
            echo "📊 サービス: ${{ env.SERVICE_NAME }}"
            echo "🏷️ イメージ: ${{ github.sha }}"
            echo "🌐 モード: live（ライブトレード）"
            echo "📈 品質保証: 55特徴量・Python 3.13"
          else
            echo "❌ デプロイ失敗"
            echo "🔍 ログを確認してください"
          fi

# ========================================
# 設定
# ========================================

# 同時実行制御（デプロイの整合性確保）
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true