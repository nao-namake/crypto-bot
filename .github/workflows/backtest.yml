# バックテスト自動化ワークフロー
# 目的: 履歴データ収集→バックテスト実行→Markdownレポート生成
# Phase 57.13: 固定期間モード対応

name: Backtest Automation

on:
  # 手動実行専用（週次スケジュールは一旦無効化）
  workflow_dispatch:
    inputs:
      backtest_days:
        description: 'バックテスト日数（固定期間モード時は無視）'
        required: false
        default: '180'
        type: choice
        options:
          - '30'
          - '60'
          - '90'
          - '180'
          - '365'

env:
  PYTHON_VERSION: '3.13'
  BACKTEST_TIMEOUT_MINUTES: 300  # 5時間（履歴収集+バックテスト）

jobs:
  backtest:
    name: Backtest Execution
    env:
      HISTORICAL_DATA_DAYS: ${{ github.event.inputs.backtest_days || '180' }}
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 全体タイムアウト: 6時間

    permissions:
      contents: write  # Git commitとpushに必要

    steps:
      # ========================================
      # Step 1: コードチェックアウト
      # ========================================
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 全履歴取得（Git操作用）

      # ========================================
      # Step 2: Python環境セットアップ
      # ========================================
      - name: Set up Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: 'requirements.txt'

      # ========================================
      # Step 3: 依存関係インストール
      # ========================================
      - name: Install dependencies
        run: |
          echo "📦 依存関係インストール開始..."
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          echo "✅ 依存関係インストール完了"

      # ========================================
      # Step 4: 履歴データ収集（Phase 57.13: 固定期間対応）
      # ========================================
      - name: Collect historical data
        run: |
          echo "📥 履歴データ収集開始..."

          # 必要なディレクトリ作成
          mkdir -p src/backtest/data/historical

          # Phase 57.13: 固定期間モード判定
          USE_FIXED=$(python3 -c "
          import yaml
          with open('config/core/thresholds.yaml') as f:
              config = yaml.safe_load(f)
          print('true' if config.get('execution', {}).get('backtest_use_fixed_dates', False) else 'false')
          ")

          if [ "$USE_FIXED" = "true" ]; then
            # 固定期間モード: 設定ファイルから日付を取得
            START_DATE=$(python3 -c "
          import yaml
          with open('config/core/thresholds.yaml') as f:
              config = yaml.safe_load(f)
          print(config.get('execution', {}).get('backtest_start_date', '2025-07-01'))
          ")
            END_DATE=$(python3 -c "
          import yaml
          with open('config/core/thresholds.yaml') as f:
              config = yaml.safe_load(f)
          print(config.get('execution', {}).get('backtest_end_date', '2025-12-31'))
          ")
            echo "📅 Phase 57.13: 固定期間モード - ${START_DATE} ~ ${END_DATE}"
            python3 src/backtest/scripts/collect_historical_csv.py --start-date "$START_DATE" --end-date "$END_DATE"
          else
            # ローリングウィンドウモード: 日数指定
            echo "📅 ローリングウィンドウモード - ${HISTORICAL_DATA_DAYS}日間"
            python3 src/backtest/scripts/collect_historical_csv.py --days ${{ env.HISTORICAL_DATA_DAYS }}
          fi

          # データ収集確認（大文字ファイル名）
          if [ -f "src/backtest/data/historical/BTC_JPY_15m.csv" ]; then
            CSV_LINES=$(wc -l < src/backtest/data/historical/BTC_JPY_15m.csv)
            echo "✅ 15分足データ収集完了: ${CSV_LINES}行"
          else
            echo "❌ 15分足データ収集失敗"
            exit 1
          fi

          if [ -f "src/backtest/data/historical/BTC_JPY_4h.csv" ]; then
            CSV_LINES=$(wc -l < src/backtest/data/historical/BTC_JPY_4h.csv)
            echo "✅ 4時間足データ収集完了: ${CSV_LINES}行"
          else
            echo "❌ 4時間足データ収集失敗"
            exit 1
          fi

          echo "✅ 履歴データ収集完了"

      # ========================================
      # Step 5: テスト環境準備
      # ========================================
      - name: Setup backtest environment
        run: |
          echo "🔧 バックテスト環境準備開始..."

          # 必要なディレクトリ作成
          mkdir -p logs cache src/backtest/logs docs/検証記録

          # モデルメタデータ作成
          echo '{"version": "backtest", "features": 55, "created": "'$(date -u +%Y-%m-%dT%H:%M:%SZ)'"}' > models/production/model_metadata.json

          # モデルファイル確認
          if [ -f "models/production/ensemble_full.pkl" ]; then
            echo "✅ ensemble_full.pkl確認済み"
          else
            echo "❌ ensemble_full.pkl未確認"
            exit 1
          fi

          if [ -f "models/production/ensemble_basic.pkl" ]; then
            echo "✅ ensemble_basic.pkl確認済み"
          else
            echo "⚠️ ensemble_basic.pkl未確認（フォールバック不可）"
          fi

          echo "✅ バックテスト環境準備完了"

      # ========================================
      # Step 6: バックテスト実行
      # ========================================
      - name: Run backtest
        run: |
          echo "🚀 バックテスト実行開始..."
          echo "実行時刻（JST）: $(TZ=Asia/Tokyo date '+%Y/%m/%d %H:%M:%S')"
          echo "実行時刻（UTC）: $(date -u '+%Y/%m/%d %H:%M:%S')"
          echo "バックテスト日数: ${{ env.HISTORICAL_DATA_DAYS }}日"

          # PYTHONPATH設定
          export PYTHONPATH="${PYTHONPATH}:$(pwd)"

          # バックテスト実行（タイムアウト対策: timeoutコマンド使用）
          timeout $((BACKTEST_TIMEOUT_MINUTES * 60)) python3 main.py --mode backtest 2>&1 | tee backtest_run.log

          BACKTEST_EXIT_CODE=${PIPESTATUS[0]}

          if [ $BACKTEST_EXIT_CODE -eq 0 ]; then
            echo "✅ バックテスト実行成功"
          elif [ $BACKTEST_EXIT_CODE -eq 124 ]; then
            echo "⏱️ バックテストタイムアウト（${BACKTEST_TIMEOUT_MINUTES}分超過）"
            exit 1
          else
            echo "❌ バックテスト実行失敗（終了コード: $BACKTEST_EXIT_CODE）"
            exit 1
          fi

          # 最新のJSONレポート確認
          LATEST_JSON=$(ls -t src/backtest/logs/backtest_*.json 2>/dev/null | head -1)
          if [ -n "$LATEST_JSON" ]; then
            echo "✅ JSONレポート生成確認: $LATEST_JSON"
            echo "LATEST_JSON=$LATEST_JSON" >> $GITHUB_ENV
          else
            echo "❌ JSONレポート未生成"
            exit 1
          fi

      # ========================================
      # Step 7: Markdownレポート生成
      # ========================================
      - name: Generate Markdown report
        run: |
          echo "📝 Markdownレポート生成開始..."

          # PYTHONPATH設定
          export PYTHONPATH="${PYTHONPATH}:$(pwd)"

          # Markdownレポート生成（日付ベースのファイル名）
          python3 scripts/backtest/generate_markdown_report.py \
            "$LATEST_JSON" \
            --output-dir "docs/検証記録"

          # 生成されたMarkdownファイル確認
          LATEST_MARKDOWN=$(ls -t docs/検証記録/backtest_*.md 2>/dev/null | head -1)
          if [ -n "$LATEST_MARKDOWN" ]; then
            echo "✅ Markdownレポート生成確認: $LATEST_MARKDOWN"
            echo "LATEST_MARKDOWN=$LATEST_MARKDOWN" >> $GITHUB_ENV
          else
            echo "❌ Markdownレポート未生成"
            exit 1
          fi

      # ========================================
      # Step 7.5: バックテスト結果をArtifactとして保存
      # ========================================
      - name: Upload backtest results
        uses: actions/upload-artifact@v4
        with:
          name: backtest-results
          path: |
            src/backtest/logs/backtest_*.json
            backtest_run.log
          retention-days: 90

      # ========================================
      # Step 8: Git設定
      # ========================================
      - name: Configure Git
        run: |
          echo "⚙️ Git設定開始..."
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "GitHub Actions Bot"
          echo "✅ Git設定完了"

      # ========================================
      # Step 9: Markdownレポートコミット
      # ========================================
      - name: Commit Markdown report
        run: |
          echo "📤 Markdownレポートコミット開始..."

          # 変更ファイル確認
          git status --short

          # Markdownレポートのみコミット（JSONは除外）
          git add "docs/検証記録/backtest_*.md"

          # コミット（変更がある場合のみ）
          if git diff --staged --quiet; then
            echo "ℹ️ コミット対象なし（変更なし）"
          else
            REPORT_DATE=$(TZ=Asia/Tokyo date '+%Y/%m/%d')
            COMMIT_MSG="docs: 週次バックテストレポート追加 ${REPORT_DATE}"
            COMMIT_BODY="週次自動バックテスト結果 | 実行日時: ${REPORT_DATE} | レポート: ${LATEST_MARKDOWN} | 自動生成: GitHub Actions"

            git commit -m "${COMMIT_MSG}" -m "${COMMIT_BODY}"
            echo "✅ コミット完了"
          fi

      # ========================================
      # Step 10: リポジトリにプッシュ
      # ========================================
      - name: Push to repository
        run: |
          echo "🚀 リポジトリにプッシュ開始..."

          # リモートの変更を取得してrebase（長時間実行中の競合対策）
          echo "📥 リモート変更を取得中..."
          git fetch origin main
          git rebase origin/main || {
            echo "⚠️ rebase競合発生 - 自動解決を試行"
            git rebase --abort
            git pull --rebase origin main
          }

          # プッシュ実行（リトライ付き）
          MAX_RETRIES=3
          RETRY_COUNT=0
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            if git push origin main; then
              echo "✅ プッシュ完了"
              break
            else
              RETRY_COUNT=$((RETRY_COUNT + 1))
              echo "⚠️ プッシュ失敗（試行 $RETRY_COUNT/$MAX_RETRIES）"
              if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
                echo "📥 再度リモート変更を取得..."
                git pull --rebase origin main
              fi
            fi
          done

          if [ $RETRY_COUNT -eq $MAX_RETRIES ]; then
            echo "❌ プッシュ失敗（${MAX_RETRIES}回リトライ後）"
            exit 1
          fi

          echo "📍 Markdownレポートがリポジトリに追加されました: ${LATEST_MARKDOWN}"
          echo "🔄 ローカル環境で 'git pull' を実行して同期してください"

      # ========================================
      # Step 11: 実行サマリー
      # ========================================
      - name: Execution summary
        if: always()
        run: |
          echo "========================================"
          echo "📊 バックテスト実行サマリー"
          echo "========================================"
          echo "レポート: ${LATEST_MARKDOWN:-N/A}"
          echo "期間: ${{ env.HISTORICAL_DATA_DAYS }}日間"

          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ ステータス: 成功"
            echo "📝 レポート: ${LATEST_MARKDOWN}"
            echo "📤 Git: コミット・プッシュ完了"
            echo "🔄 ローカル環境: git pull で同期してください"
          else
            echo "❌ ステータス: 失敗"
            echo "🔍 ログを確認してください"
          fi

          echo "========================================"
          echo "実行時刻（JST）: $(TZ=Asia/Tokyo date '+%Y/%m/%d %H:%M:%S')"
          echo "========================================"

# ========================================
# 同時実行制御
# ========================================
concurrency:
  group: weekly-backtest
  cancel-in-progress: false  # 実行中のバックテストは中断しない
